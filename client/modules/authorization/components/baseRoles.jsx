/*import React from 'react';


const baseRoles = ({baseroles}) => (
    
    <div>
        {baseroles.map(baserole => (
            <option id={baserole._id} value={baserole.role}>{baserole.description}</option>
        ))}
    </div>
    
);

export default baseRoles;*/
